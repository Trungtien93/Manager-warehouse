@model List<MNBEMART.Models.StockTransfer>
@using MNBEMART.Models
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Phiếu chuyển kho</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" />
    <style>
        @@media print {
            .no-print { display: none; }
            .page-break { page-break-after: always; }
            body { margin: 0; padding: 10px; }
            @@page {
                margin: 0.5cm;
                size: A4;
            }
        }
        @@page {
            margin: 0;
        }
        body { 
            font-family: 'Times New Roman', Times, serif; 
            font-size: 12px;
        }
        .print-container {
            max-width: 100%;
            margin: 0 auto;
        }
        .header-compact {
            text-align: center;
            margin-bottom: 12px;
            padding-bottom: 8px;
        }
        .header-title {
            font-size: 16px;
            font-weight: bold;
            margin: 3px 0;
        }
        .header-info {
            font-size: 10px;
            margin: 2px 0;
        }
        .table {
            width: 100%;
            border-collapse: collapse;
            margin-top: 8px;
            margin-bottom: 15px;
        }
        .table th {
            background-color: #f5f5f5;
            border: 1px solid #000;
            padding: 5px 3px;
            text-align: center;
            font-weight: bold;
            font-size: 10px;
        }
        .table td {
            border: 1px solid #000;
            padding: 4px 3px;
            font-size: 10px;
        }
        .table tfoot th {
            background-color: #f5f5f5;
            font-weight: bold;
        }
        .signature-section {
            margin-top: 20px;
            padding-top: 10px;
        }
        .signature-box {
            text-align: center;
            display: inline-block;
            width: 30%;
            vertical-align: top;
        }
        .signature-box .title {
            font-weight: bold;
            font-size: 11px;
            margin-bottom: 3px;
        }
        .signature-box .subtitle {
            font-size: 9px;
            color: #666;
            margin-bottom: 40px;
        }
        .signature-box .name {
            font-size: 10px;
            padding-top: 3px;
            margin-top: 40px;
        }
    </style>
</head>
<body>
    <div class="print-container">
        <div class="no-print mb-3 text-end">
            <button class="btn btn-primary" onclick="window.print()">🖨️ In</button>
            <a class="btn btn-outline-secondary" asp-action="Index">Quay lại</a>
        </div>

        @foreach (var receipt in Model)
        {
            decimal totalQty = receipt.Details?.Sum(d => d.Quantity) ?? 0m;
            
            <div class="receipt @(receipt != Model.Last() ? "page-break" : "")">
                <div class="header-compact">
                    <div class="header-title">PHIẾU CHUYỂN KHO</div>
                    <div class="header-info">
                        Số: <strong>@receipt.TransferNumber</strong> | 
                        Ngày: @receipt.CreatedAt.ToLocalTime().ToString("dd/MM/yyyy") | 
                        Kho xuất: <strong>@receipt.FromWarehouse?.Name</strong> → 
                        Kho nhập: <strong>@receipt.ToWarehouse?.Name</strong>
                    </div>
                </div>

                <table class="table table-bordered">
                    <thead>
                        <tr>
                            <th style="width: 5%">STT</th>
                            <th style="width: 12%">Mã vật tư</th>
                            <th style="width: 30%">Tên vật tư</th>
                            <th style="width: 10%">NSX</th>
                            <th style="width: 10%">HSD</th>
                            <th style="width: 8%">ĐVT</th>
                            <th style="width: 10%" class="text-end">Số lượng</th>
                            <th style="width: 13%">Ghi chú</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{int stt = 1;}
                        @foreach (var d in receipt.Details ?? Enumerable.Empty<StockTransferDetail>())
                        {
                            <tr>
                                <td class="text-center">@stt</td>
                                <td>@d.Material?.Code</td>
                                <td>@d.Material?.Name</td>
                                <td class="text-center">@(d.ManufactureDate?.ToString("dd/MM/yyyy") ?? (d.Material?.ManufactureDate?.ToString("dd/MM/yyyy") ?? "-"))</td>
                                <td class="text-center">@(d.ExpiryDate?.ToString("dd/MM/yyyy") ?? (d.Material?.ExpiryDate?.ToString("dd/MM/yyyy") ?? "-"))</td>
                                <td class="text-center">@d.Unit</td>
                                <td class="text-end">@d.Quantity.ToString("0.##")</td>
                                <td>@(string.IsNullOrWhiteSpace(d.Note) ? "-" : d.Note)</td>
                            </tr>
                            stt++;
                        }
                    </tbody>
                    <tfoot>
                        <tr>
                            <th colspan="6" class="text-end">Tổng cộng:</th>
                            <th class="text-end">@totalQty.ToString("0.##")</th>
                            <th></th>
                        </tr>
                    </tfoot>
                </table>

                <div class="signature-section">
                    <div style="display: flex; justify-content: space-between;">
                        <div class="signature-box">
                            <div class="title">Người lập phiếu</div>
                            <div class="subtitle">(Ký, họ tên)</div>
                            <div class="name">@receipt.CreatedBy?.FullName</div>
                        </div>
                        <div class="signature-box">
                            <div class="title">Thủ kho xuất</div>
                            <div class="subtitle">(Ký, họ tên)</div>
                            <div class="name"></div>
                        </div>
                        <div class="signature-box">
                            <div class="title">Thủ kho nhập</div>
                            <div class="subtitle">(Ký, họ tên)</div>
                            <div class="name"></div>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</body>
</html>
